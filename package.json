{
  "name": "jochero-monorepo",
  "version": "1.0.0",
  "description": "JOCHero Monorepo - API Backend",
  "scripts": {
    "postinstall": "npm install --prefix product/backend",
    "build": "npm run db:generate --prefix product/backend",
    "start": "npm start --prefix product/backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
