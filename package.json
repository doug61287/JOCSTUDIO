{
  "name": "jochero-monorepo",
  "version": "1.0.0",
  "description": "JOCHero Monorepo - API Backend",
  "scripts": {
    "postinstall": "cd product/backend && npm install",
    "build": "cd product/backend && npx prisma generate",
    "start": "cd product/backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
